machine:
  services:
    - docker
    - rabbitmq-server
    - redis

dependencies:
  override:
    # Don't do the default Railsy stuff (CircleCI).
    - ls

database:
  override:
    # Don't do the default Railsy stuff (CircleCI).
    - ls

test:
  override:
    - ./bin/run_changed_tests.rb
