machine:
  services:
    - docker
    - rabbitmq-server
    - redis

dependencies:
  override:
    # Don't do the default Railsy stuff (CircleCI).
    - echo "Do nothing." > /dev/null

test:
  override:
    - ./bin/run_changed_tests.rb
