machine:
  services:
    - docker
    - rabbitmq-server
    - redis

dependencies:
  override:
    # Don't do the default Railsy stuff (CircleCI).
    - true

database:
  override:
    # Don't do the default Railsy stuff (CircleCI).
    - true

test:
  override:
    - ./bin/run_changed_tests.rb
