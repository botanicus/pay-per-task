machine:
  services:
    - docker
    - rabbitmq-server
    - redis

dependencies:
  pre:
    - sudo apt-get update
  override:
    # Don't do the default Railsy stuff (CircleCI).
    - echo "Do nothing."

test:
  override:
    - ./bin/run_changed_tests.rb
